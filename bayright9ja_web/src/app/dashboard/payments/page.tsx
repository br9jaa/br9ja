export default function PaymentsPage() {
  return (
    <section className="rounded-[2rem] border border-white/10 bg-[#071a31] p-6 text-white">
      <p className="text-sm font-semibold uppercase tracking-[0.28em] text-[var(--color-neon-gold)]">
        Payments
      </p>
      <h1 className="mt-3 text-3xl font-bold">Web payment flows are ready for the next phase</h1>
      <p className="mt-4 max-w-2xl text-sm leading-7 text-white/65">
        This route now lives inside the secure dashboard shell, with balance,
        profile state, and responsive sidebar behavior already synced.
      </p>
    </section>
  );
}
